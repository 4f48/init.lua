This is my init.lua, mainly here so I don't lose it, but you can use it for yourself too.
Installing lazygit on your machine is required for lazygit.nvim to work correctly. A `setup.sh` script is included to automatically install everything needed on DNF (RHEL/Fedora) based systems.
